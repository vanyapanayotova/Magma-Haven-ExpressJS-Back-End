<section class="volcano-catalog">
    <h1 id="title"><span>Volcano</span> Catalog</h1>
    <div class="item-list">

        {{#each volcanoes}}
        <div class="volcano">
            <div class="volcano-img">
                <img src="{{imageUrl}}">
            </div>
            <div class="volcano-info">
                <h1>{{name}}</h1>
                <p><span>Location: </span>{{location}}</p>
                <p><span>Volcano Type: </span>{{volcanoType}}</p>
            </div>

            <a href="/volcanoes/{{_id}}" class="btn-details">Details</a>
        </div>
        {{else}}
        <div class="no-volcano">
            <p>There are no volcano found yet!</p>
        </div>
        {{/each}}
    </div>
</section>